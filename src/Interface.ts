//Interface declaration
interface user{
    name:string,
    age:number
}
let guest:user={name:"abd",age:32} // To use the interface, all propoerties must be assigned 
console.log(guest);//this will return json data
console.log(guest.age);

